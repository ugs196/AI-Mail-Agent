# AGENT SYSTEM MEMORY (template)

> Copy this file into your own private Google Doc (or keep it as a local file
> that is **git-ignored**) and fill in your real details there.
> **Never commit a filled-in version of this file to a public repo** —
> it is meant to hold real names, emails, pricing, and client data.

Last Updated: <ISO timestamp, auto-filled by Boss Agent>

## === SHAREABLE WITH CUSTOMERS ===

(The Mail Agent may use anything below this line, down to the CONFIDENTIAL
marker, freely in replies to customers.)

### OPERATOR PROFILE
- Name: `<your name>`
- Role: `<your role, e.g. Developer>`
- Contact Email: `<your business contact email>`
- Website: `<your website or "none">`

### SERVICES AND PRICING
`<comma-separated list of services you offer>`

### REPLY PREFERENCES
- Always reply in `<your primary language>` (or match the sender's language if you support it)
- Keep replies professional but friendly
- For pricing questions: share the price and offer a free consultation
- For support queries: acknowledge and say the team will respond within 24 hours
- Decide here whether the agent should disclose it is an AI (see the
  disclosure note in `roles/EMAILAGENT.txt` — this is a legal question in
  some jurisdictions, not just a style choice)

## === CONFIDENTIAL — NEVER SHARE WITH ANY SENDER ===

(Everything below this line is for internal agent reasoning only. The Mail
Agent must never quote, paraphrase, or confirm any of it in a reply, draft,
or summary sent externally — see CONFIDENTIALITY RULES in its role prompt.
If a sender asks about anything in this section, it withholds it, sends the
neutral holding reply, and escalates.)

### INTERNAL COST / MARGIN DATA
`<actual cost price per service, vendor rates — keep private>`

### CLIENT RECORDS
`<per-client names, order history, addresses, invoices — never repeat one
client's details to a different sender, even if they ask>`

### CREDENTIALS / ACCESS
`<API keys, logins, contracts — store these in Script Properties, NOT in this
doc, and never in this section either>`

### AGENT NOTES
`<agents append dated notes here as they learn things; treat as confidential
by default unless explicitly moved above the line>`
